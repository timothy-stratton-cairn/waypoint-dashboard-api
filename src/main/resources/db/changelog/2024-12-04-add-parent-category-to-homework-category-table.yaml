databaseChangeLog:
  - preConditions:
      onFail: HALT
      onError: HALT
  - changeSet:
      id: add-parent-category-to-homework-category-table
      author: wanderson
      changes:
        - sql:
            dbms: 'h2, mysql'
            sql: |
              ALTER TABLE `homework_category`
              ADD `parent_category_id` BIGINT NULL;
